import { login } from '../actions/userActions';
export const loginUser = (email, password) => { 
    return (dispatch) => {
// Make API call to authenticate user
// On success, dispatch the login action with the user's details dispatch(login({ email, name: 'John Doe' }));
}; 
};